function calcNewLevelAndXP( currentLevel, currentXP, gainedXP) {
    let leftOverXP = 0;
    do {
        switch(currentLevel){
            
        }
    }
    while( leftOverXP < 0 )
}

export default calcNewLevelAndXP;